import React, { FC } from 'react'

interface ILoading {
    isActive: boolean
}

const Loading: FC<ILoading> = ({ isActive }) => {
    return (
        <div>


        </div>)
}

export default Loading